#include "main.h"

/**
*mul - multipli to digit
*@a: Number to multipli
*@b: Number to multipli
*Return: 1 if is upper, 0 otherwise
*/
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
