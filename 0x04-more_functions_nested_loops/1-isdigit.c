#include "main.h"

/**
*_isupper - Check if a number is upper or lower
*@c: Number to check
*Return: 1 if is upper, 0 otherwise
*/
int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{
		return (1);
	}
	else
	{
		return (0);
	} 
}
